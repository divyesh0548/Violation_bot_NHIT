VRN Lookup : 
Let's create a new script from scratch. 
I have a vrn file. 
First of all remove all duplicate entries in "Veh Reg No.". 

There is a column named "TC Class". Apply some filters in that, remove all rows with "CarJeep" and "OSV", values.

Now another column "Payment Method". Remove all rows with "Exempt" values. 

Then from resulted data take "Veh Reg No." column in which number plate data is stored.

Compare them with a table named "checkpostmaster" in amazon cloud with database name "nhit". 

Create a result excel file which stores found and not found "vehicle no". In found vehicle numbers sheet, fetch weights for that vehicle from a column name "weight", which is available in "checkpostmaster" table. 

Create another sheet for not found vehicle numbers and store them.

Here is how I connect to database. 

def get_db_connection():
    """Create and return a PostgreSQL database connection"""
    try:
        conn = psycopg2.connect(
            host=DB_HOST,
            port=DB_PORT,
            database=DB_NAME,
            user=DB_USER,
            password=DB_PASSWORD
        )
        return conn
    except psycopg2.Error as e:
        print(f"Error connecting to database: {e}")
        return None




Dashboard : 
I want to create a dashboard which displays who submitted form, and who did not. 

This is the submissions table structure.

CREATE TABLE
  public.submissions (
    id serial NOT NULL,
    entity_name character varying(100) NOT NULL,
    name character varying(100) NOT NULL,
    email character varying(120) NOT NULL,
    date date NOT NULL,
    shift character varying(20) NOT NULL,
    vrn_file_url character varying(500) NULL,
    etc_file_url character varying(500) NULL,
    created_at timestamp without time zone NULL DEFAULT CURRENT_TIMESTAMP
  );

ALTER TABLE
  public.submissions
ADD
  CONSTRAINT submissions_pkey PRIMARY KEY (id)

I want to develop this dashboard in my flask application.

Create a table like structure. There are 40 entities who can submit the form. 
So a list them vertically. 

Get their name from another table "email_data", and column "plaza_name". 

Now, header will be like this. On top display 3 dates, today, yesterday, and a day before that.

Below them show three shifts. (All dates will have 3 shifts).

Below them show two file names "VRN" and "ETC". (All Shifts will have two sub-columns)

Shifts are




Index : 
@class_validation.py I want to add some changes in this program. First of all each weight range have an index, which is here. less than or equal to 7500 = 1, grt 7500 and lst or equal to 12,000 = 2, grt 12,000 and lst or equal to 18,500 = 3, grt 18,500 and less than or equal to 28,000 = 4, grt 28,000 and lst or equal to 60,000 = 5, grt 60,000 = 6.   Now I also have Index for some strings, which I want to map. Here it is = (1 = Car), (2 = LCV, Minibus), (3 = Truck, Bus), (4 = Truck3X), (5 = MAV). Now vrn file will have one column "TC Class", where these strings will be saved, so acording to mapping fetch their index number and save them into a new column "TC_class_index".  Then this program will create a column which is "Weight_Based_Class". In which it will store som strings. So map those strings and save their index in another column "Correct_index". After that just perform simple subtraction from "Correct_index" to "TC_class_index". And save this subtraction result in another column named "result"







VRN automation : 
I have attached a python automation script that takes vrn_file.xlsx as input. 

I want to run it for multiple vrn_files. I have a table named "checkpostmaster", where a column named "vrn_file_url" stores the links of vrn_files. I want to develop a script that takes this links download vrn file from it and pass it to this script.

Then after completion update a column named "file_status" to 1 in checkpostmaster.

Then go to next link.

Here is the example link : "https://snt-nhit-data.s3.us-east-1.amazonaws.com/madai/ETC/12_8/20251124_052808_TransactionHistoryLogReport__Shift-A_24112025.xlsx"

One thing is that after downloading the file rename it to "vrn_file.xlsx". So that it can be passed to my program.

After updating the "file_status" column delete the downloaded file. 

My program will generate an excel file. which is this = 'vrn_file_validated.xlsx'. I want to rename it. 

Fetch date from "date" column and shift from "shift" column for that row from table and add these two in file name. So it will be easy to identify.

Then upload this file to amazon S3 platform. 
I already have a function to store them.

def upload_file_to_s3(file, entity_name, file_type, shift):
    try:
        # Generate secure filename with timestamp
        timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
        original_filename = secure_filename(file.filename)
        filename = f"{timestamp}_{original_filename}"
        
        # Construct S3 key (path)
        s3_key = f"{entity_name}/{file_type}/{shift}/{filename}"
        
        # Upload file to S3
        if not s3_client:
            raise Exception("S3 client not initialized. Check AWS credentials.")
        
        s3_client.upload_fileobj(
            file,
            AWS_S3_BUCKET_NAME,
            s3_key,
            ExtraArgs={
                'ContentType': file.content_type,
                'ContentDisposition': 'inline'  # For viewing in browser
            }
        )
        
        # Generate S3 URL
        s3_url = f"https://{AWS_S3_BUCKET_NAME}.s3.{AWS_REGION}.amazonaws.com/{s3_key}"
        
        return s3_url
        
    except NoCredentialsError:
        print("AWS credentials not available")
        return None
    except ClientError as e:
        print(f"Error uploading to S3: {e}")
        return None
    except Exception as e:
        print(f"Unexpected error: {e}")
        return None

One thing is that create a folder "Output_file" and then store them as provided in function.

After that this function will return url of file, so simply store it back to the table in column name "output_link".

For now develop flow for this.





Result email : @automation.py Create another function which will run after all step 7 are completed. It will check whole submissions table for pending emails. Here is the condition. "email_sent" column must be empty or 0. and "file_status" must be one. This is the only case for pending email. If a record found then return it's id.  Now create another function which will fetch necessary data for email triggering using this id. First of all fetch "email", "entity_name", "shift", "date" from submissions table. then using entity_name fetch another email from column "email_id" in email_data table. Compare entity_name with plaza_name, which is in email_data tabel. 





for files 24 HOURs : Lest's just make it simple. I have date which is in format 29-11-2025. And shifts. First program will check which shift is current shift. If it is (8_16), then it will process for files with date 28-11-2025 and shift (8_16). Because for shift (8_16) files will be uploaded after 16.00 PM. 